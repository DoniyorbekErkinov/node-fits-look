// Modules
// CommonJS, every file is module (by default)
// Module - Encapsulated Code (only share minimum)

const names = require('./04-names');
const sayHi = require('./05-utils');
const data = require('./06-alternative')
require('./07--mine-granate')
// console.log(data);
// console.log(names);
// sayHi('doniyor')
// sayHi(names.name2)
// sayHi(names.name3)